-- INNER JOINの例, 条件に合ったレコードをだけを結像して表示する

SELECT 
   sp2.id, 
   sp2.family_name, 
   sp2.first_name, 
   sp2.age, 
   s.name AS sport_name
FROM 
   student_profiles2 sp2
INNER JOIN 
   sports s ON sp2.sports = s.id;

